name: Matrix Workflow
on: push
jobs:
    matrix-command:
        strategy:
            matrix:
                os: [ubuntu-latest, windows-latest]
                node-version: [18, 20, 24]
        runs-on: ${{ matrix.os }}
        steps:
            - run: node -v
            - uses: actions/setup-node@v6
              # launch the workflow with different versions of node simultaneously
              with:
                  node-version: ${{ matrix.node-version }}
            - run: node -v
