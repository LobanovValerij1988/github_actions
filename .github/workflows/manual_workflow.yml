name: Manual Workflow
on:
    workflow_dispatch:
        inputs:
            test_input:
                description: "This is a test input for the manual workflow"
                type: string
                required: true
                default: "Default value for test input"
jobs:
    manual-command:
        runs-on: ubuntu-latest
        steps:
            - name: manual command
              run: echo "Manual workflow executed!"
