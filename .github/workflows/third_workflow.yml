name: Copy Repository Workflow
on: push
jobs:
    copy-repository:
        runs-on: ubuntu-latest
        steps:
            - name: list files in current directory
              run: ls -a
            - uses: actions/checkout@v4
            - name: list files in current directory after copying repositorys
              run: ls -a
