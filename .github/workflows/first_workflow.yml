name: First Workflow
on: push
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: first command
        run: echo "Hello, World!"
      - name: multi-line script
        run: |-
          node -v
          npm -v
