name: First Workflow
on: workflow_dispatch
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: first command
        run: echo "Hello, World!"
      - name: multi-line script
        run: |-
          node -v
          npm -v
